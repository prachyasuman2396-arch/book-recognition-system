"""Book Recognition & Recommendation System.

Production-grade multi-agent pipeline built on LangGraph + FastAPI that
detects books in an image, validates them against the Google Books API,
and returns ranked recommendations.
"""

__version__ = "1.0.0"
